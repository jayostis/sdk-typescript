# Spec diagnostics (fixture template)

FIXTURE-TEMPLATE-MARKER-7f3a — this line exists only in the fixture template, so a renderer that
inlined its markdown instead of reading this file cannot produce it.
